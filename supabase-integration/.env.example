# Public — safe to ship in frontend code (protected by RLS, not secrecy)
VITE_SUPABASE_URL=https://dkkpolnuywgvmlacjzto.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_AItCimP_thtUqcy2e3kp9Q_4TemAAN4

# SECRET — server-side only (Edge Function env, never the frontend bundle).
# Bypasses RLS entirely. Get it from Project Settings → API → service_role.
# Needed later for the Razorpay webhook that verifies payment and flips
# entries.payment_status to PAID server-side.
# SUPABASE_SERVICE_ROLE_KEY=
